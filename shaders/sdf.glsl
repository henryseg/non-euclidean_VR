/**
 * Return the distance (or some underestimation of it) to the object, along the geodesic directed by v.
 * This function is just a model.
 * It need be implement for each kind of object.
 * @todo Should we include with the distance an optional computation of the normal?
 * It could simplify the lighting computation…
 */
float objetSDF(Vector v){}
