<?xml version="1.0" encoding="UTF-8"?>
<library>
    <background>
        // auxiliary shader code, e.g. distance between tow points for the ball sdf
    </background>
    <items>
        <solid class="solidClassName">
            <shader type="declare">
                // code used to declare the solid
            </shader>
            <shader type="setup">
                // code used to setup the solid
            </shader>
            <shader type="sdf">
                // body of the corresponding sdf or distance underestimator
                // the wrapping into a function is done by the template engine
                // be careful with global objects (see examples)
            </shader>
            <shader type="gradient">
               // body of the gradient of the sdf
               // the wrapping into a function is done by the template engine
               // be careful with global objects (see examples)
            </shader>
        </solid>
    </items>
</library>